import React from "react";
import MenuCard from "./MenuCard";
import burger1 from "../../assets/burger1.png";
import burger2 from "../../assets/burger2.png";
import burger3 from "../../assets/burger3.png";

const Menu = () => {
  const addToCartHandler = (itemNum) => {};

  return (
    <section id="menu">
      <h1>MENU</h1>

      <div>
    // A menu card of the items with the item number, image source, price, title, a function handler for adding to cart, and a delay for animation.
      </div>
    </section>
  );
};

export default Menu;
